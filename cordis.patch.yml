# Add this row to $DSH_HOME/profiles/web/cordis.patch.yml after installing this package.
- insert:
    - id: subagent-workspace-manager
      name: 'dsh-subagent-workspace-ui'

# Disable the stock lineage dropdown while this manager is installed.
# Removing this bundle restores the user's underlying ui-subagent setting.
- id: ui-subagent
  disabled: true
